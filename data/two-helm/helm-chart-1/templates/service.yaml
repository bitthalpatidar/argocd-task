apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-1.fullname" . }}
spec:
  selector:
    app: {{ include "helm-chart-1.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
