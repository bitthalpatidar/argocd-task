apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-chart-2.fullname" . }}
spec:
  selector:
    app: {{ include "helm-chart-2.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 80
