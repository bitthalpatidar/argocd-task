apiVersion: batch/v1
kind: Job
metadata:
  name: testjob
spec:
  backoffLimit: 1
  completions: 1
  template:
    spec:
      containers:
      - name: cont1
        image: busybox
        command: ["sh", "-c", "for i in $(seq 1 10); do echo 'mypod is running'; done"]
      restartPolicy: Never
