apiVersion: batch/v1
kind: Job
metadata:
  name: testjob
spec:
  backoffLimit: 1
  completions: 1
  template:
    spec:
      containers:
      - name: cont1
        image: busybox
        command: ["/bin/bash","-c","while true;  do echo mypod is running; sleep 5; done"]
      restartPolicy: Never
