apiVersion: batch/v1
kind: Job
metadata:
  name: testjob
spec:
  backoffLimit: 1
  completions: 1
  template:
    spec:
      containers:
      - name: cont1
        image: busybox
        command: ["echo", "This is a test job"]
      restartPolicy: Never
